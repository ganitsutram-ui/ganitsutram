[dotenv@17.3.1] injecting env (14) from ..\config\.env -- tip: ­ƒøí´©Å auth for agents: https://vestauth.com
[DB] Using SQLite Adapter
[
  {
    "name": "users",
    "sql": "CREATE TABLE users (\n            user_id       TEXT PRIMARY KEY,\n            email         TEXT UNIQUE NOT NULL,\n            password_hash TEXT NOT NULL,\n            role          TEXT NOT NULL,\n            created_at    TEXT NOT NULL\n        )"
  },
  {
    "name": "progress",
    "sql": "CREATE TABLE progress (\n            progress_id   TEXT PRIMARY KEY,\n            user_id       TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            operation     TEXT NOT NULL,\n            input         TEXT,\n            input_a       TEXT,\n            input_b       TEXT,\n            result        TEXT NOT NULL,\n            steps         TEXT,\n            solved_at     TEXT NOT NULL,\n            time_taken_ms INTEGER\n        )"
  },
  {
    "name": "sessions",
    "sql": "CREATE TABLE sessions (\n            session_id    TEXT PRIMARY KEY,\n            user_id       TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            created_at    TEXT NOT NULL,\n            expires_at    TEXT NOT NULL\n        )"
  },
  {
    "name": "practice_attempts",
    "sql": "CREATE TABLE practice_attempts (\n            attempt_id     TEXT PRIMARY KEY,\n            user_id        TEXT REFERENCES users(user_id) ON DELETE CASCADE,\n            operation      TEXT NOT NULL,\n            question       TEXT NOT NULL,\n            correct_answer TEXT NOT NULL,\n            user_answer    TEXT,\n            is_correct     INTEGER NOT NULL,\n            difficulty     TEXT NOT NULL,\n            attempted_at   TEXT NOT NULL,\n            time_taken_ms  INTEGER\n        )"
  },
  {
    "name": "discoveries",
    "sql": "CREATE TABLE discoveries (\n            discovery_id   TEXT PRIMARY KEY,\n            slug          TEXT UNIQUE NOT NULL,\n            title         TEXT NOT NULL,\n            icon          TEXT NOT NULL,\n            sutra         TEXT,\n            sutra_meaning TEXT,\n            description   TEXT NOT NULL,\n            long_desc     TEXT,\n            example_input TEXT,\n            example_output TEXT,\n            category      TEXT NOT NULL,\n            difficulty    TEXT NOT NULL,\n            sort_order    INTEGER DEFAULT 0,\n            created_at    TEXT NOT NULL\n        )"
  },
  {
    "name": "discovery_patterns",
    "sql": "CREATE TABLE discovery_patterns (\n            pattern_id    TEXT PRIMARY KEY,\n            discovery_id  TEXT NOT NULL REFERENCES discoveries(discovery_id) ON DELETE CASCADE,\n            pattern_label TEXT NOT NULL,\n            pattern_desc  TEXT NOT NULL,\n            example       TEXT\n        )"
  },
  {
    "name": "schools",
    "sql": "CREATE TABLE schools (\n            school_id   TEXT PRIMARY KEY,\n            name        TEXT NOT NULL,\n            admin_id    TEXT REFERENCES users(user_id) ON DELETE SET NULL,\n            student_cap INTEGER DEFAULT 100,\n            created_at  TEXT NOT NULL\n        )"
  },
  {
    "name": "enrollments",
    "sql": "CREATE TABLE enrollments (\n            enrollment_id TEXT PRIMARY KEY,\n            school_id     TEXT NOT NULL REFERENCES schools(school_id) ON DELETE CASCADE,\n            user_id       TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            enrolled_at   TEXT NOT NULL,\n            status        TEXT DEFAULT 'active'\n        )"
  },
  {
    "name": "reset_tokens",
    "sql": "CREATE TABLE reset_tokens (\n            token_id    TEXT PRIMARY KEY,\n            user_id     TEXT REFERENCES users(user_id) ON DELETE CASCADE,\n            token_hash  TEXT NOT NULL UNIQUE,\n            expires_at  TEXT NOT NULL,\n            used        INTEGER DEFAULT 0,\n            created_at  TEXT NOT NULL\n        )"
  },
  {
    "name": "refresh_sessions",
    "sql": "CREATE TABLE refresh_sessions (\n            session_id       TEXT PRIMARY KEY,\n            user_id          TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            refresh_token_hash TEXT NOT NULL UNIQUE,\n            family_id        TEXT NOT NULL,\n            device_hint      TEXT,\n            ip_hint          TEXT,\n            issued_at        TEXT NOT NULL,\n            expires_at       TEXT NOT NULL,\n            last_used_at     TEXT,\n            rotated          INTEGER DEFAULT 0,\n            revoked          INTEGER DEFAULT 0\n        )"
  },
  {
    "name": "analytics_events",
    "sql": "CREATE TABLE analytics_events (\n            event_id     TEXT PRIMARY KEY,\n            event_type   TEXT NOT NULL,\n            user_id      TEXT,\n            session_id   TEXT,\n            operation    TEXT,\n            metadata     TEXT,\n            ip_hint      TEXT,\n            user_agent   TEXT,\n            created_at   TEXT NOT NULL\n        )"
  },
  {
    "name": "user_badges",
    "sql": "CREATE TABLE user_badges (\n            user_badge_id  TEXT PRIMARY KEY,\n            user_id        TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            badge_id       TEXT NOT NULL,\n            earned_at      TEXT NOT NULL,\n            notified       INTEGER DEFAULT 0\n        )"
  },
  {
    "name": "user_scores",
    "sql": "CREATE TABLE user_scores (\n            score_id       TEXT PRIMARY KEY,\n            user_id        TEXT NOT NULL UNIQUE REFERENCES users(user_id) ON DELETE CASCADE,\n            display_name   TEXT,\n            total_points   INTEGER DEFAULT 0,\n            weekly_points  INTEGER DEFAULT 0,\n            monthly_points INTEGER DEFAULT 0,\n            streak         INTEGER DEFAULT 0,\n            rank_global    INTEGER,\n            rank_weekly    INTEGER,\n            last_updated   TEXT NOT NULL\n        )"
  },
  {
    "name": "point_events",
    "sql": "CREATE TABLE point_events (\n            event_id       TEXT PRIMARY KEY,\n            user_id        TEXT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,\n            points         INTEGER NOT NULL,\n            reason         TEXT NOT NULL,\n            operation      TEXT,\n            created_at     TEXT NOT NULL\n        )"
  },
  {
    "name": "cms_content",
    "sql": "CREATE TABLE cms_content (\n            content_id     TEXT PRIMARY KEY,\n            content_type   TEXT NOT NULL,\n            slug           TEXT UNIQUE NOT NULL,\n            title_en       TEXT NOT NULL,\n            title_hi       TEXT,\n            title_sa       TEXT,\n            body_en        TEXT NOT NULL,\n            body_hi        TEXT,\n            body_sa        TEXT,\n            excerpt_en     TEXT,\n            excerpt_hi     TEXT,\n            excerpt_sa     TEXT,\n            icon           TEXT,\n            category       TEXT,\n            difficulty     TEXT,\n            tags           TEXT,\n            sort_order     INTEGER DEFAULT 0,\n            published      INTEGER DEFAULT 0,\n            featured       INTEGER DEFAULT 0,\n            author_id      TEXT REFERENCES users(user_id),\n            created_at     TEXT NOT NULL,\n            updated_at     TEXT NOT NULL,\n            published_at   TEXT\n        )"
  },
  {
    "name": "cms_revisions",
    "sql": "CREATE TABLE cms_revisions (\n            revision_id    TEXT PRIMARY KEY,\n            content_id     TEXT REFERENCES cms_content(content_id) ON DELETE CASCADE,\n            author_id      TEXT REFERENCES users(user_id),\n            title_en       TEXT,\n            body_en        TEXT,\n            change_summary TEXT,\n            created_at     TEXT NOT NULL\n        )"
  },
  {
    "name": "cms_media",
    "sql": "CREATE TABLE cms_media (\n            media_id       TEXT PRIMARY KEY,\n            content_id     TEXT REFERENCES cms_content(content_id) ON DELETE CASCADE,\n            media_type     TEXT,\n            label          TEXT,\n            value          TEXT,\n            sort_order     INTEGER DEFAULT 0\n        )"
  },
  {
    "name": "notifications",
    "sql": "CREATE TABLE notifications (\n            notification_id  TEXT PRIMARY KEY,\n            user_id          TEXT REFERENCES users(user_id) ON DELETE CASCADE,\n            type             TEXT NOT NULL,\n            title            TEXT NOT NULL,\n            body             TEXT NOT NULL,\n            icon             TEXT,\n            action_url       TEXT,\n            action_label     TEXT,\n            read             INTEGER DEFAULT 0,\n            created_at       TEXT NOT NULL\n        )"
  },
  {
    "name": "notification_prefs",
    "sql": "CREATE TABLE notification_prefs (\n            user_id           TEXT PRIMARY KEY REFERENCES users(user_id) ON DELETE CASCADE,\n            badge_earned      INTEGER DEFAULT 1,\n            rank_up           INTEGER DEFAULT 1,\n            rank_milestone    INTEGER DEFAULT 1,\n            streak_milestone  INTEGER DEFAULT 1,\n            practice_result   INTEGER DEFAULT 0,\n            content_published INTEGER DEFAULT 1,\n            system            INTEGER DEFAULT 1,\n            weekly_reset      INTEGER DEFAULT 1,\n            points_awarded    INTEGER DEFAULT 0,\n            email_badge       INTEGER DEFAULT 1,\n            email_milestone   INTEGER DEFAULT 1,\n            email_system      INTEGER DEFAULT 0\n        )"
  },
  {
    "name": "search_index",
    "sql": "CREATE TABLE search_index (\n            doc_id      TEXT PRIMARY KEY,\n            doc_type    TEXT NOT NULL,\n            slug        TEXT,\n            title       TEXT NOT NULL,\n            body        TEXT,\n            tags        TEXT,\n            category    TEXT,\n            locale      TEXT DEFAULT 'en',\n            weight      INTEGER DEFAULT 1,\n            updated_at  TEXT NOT NULL\n        )"
  },
  {
    "name": "search_fts",
    "sql": "CREATE VIRTUAL TABLE search_fts USING fts5(\n            doc_id UNINDEXED,\n            doc_type UNINDEXED,\n            slug UNINDEXED,\n            title,\n            body,\n            tags,\n            category UNINDEXED,\n            locale UNINDEXED,\n            content=search_index,\n            content_rowid=rowid\n        )"
  },
  {
    "name": "search_fts_data",
    "sql": "CREATE TABLE 'search_fts_data'(id INTEGER PRIMARY KEY, block BLOB)"
  },
  {
    "name": "search_fts_idx",
    "sql": "CREATE TABLE 'search_fts_idx'(segid, term, pgno, PRIMARY KEY(segid, term)) WITHOUT ROWID"
  },
  {
    "name": "search_fts_docsize",
    "sql": "CREATE TABLE 'search_fts_docsize'(id INTEGER PRIMARY KEY, sz BLOB)"
  },
  {
    "name": "search_fts_config",
    "sql": "CREATE TABLE 'search_fts_config'(k PRIMARY KEY, v) WITHOUT ROWID"
  },
  {
    "name": "graph_nodes",
    "sql": "CREATE TABLE graph_nodes (\n            node_id     TEXT PRIMARY KEY,\n            node_type   TEXT NOT NULL,\n            label       TEXT NOT NULL,\n            label_hi    TEXT,\n            label_sa    TEXT,\n            description TEXT,\n            icon        TEXT,\n            category    TEXT,\n            url         TEXT,\n            weight      INTEGER DEFAULT 1,\n            metadata    TEXT\n        )"
  },
  {
    "name": "graph_edges",
    "sql": "CREATE TABLE graph_edges (\n            edge_id     TEXT PRIMARY KEY,\n            source_id   TEXT REFERENCES graph_nodes(node_id) ON DELETE CASCADE,\n            target_id   TEXT REFERENCES graph_nodes(node_id) ON DELETE CASCADE,\n            edge_type   TEXT NOT NULL,\n            label       TEXT,\n            weight      INTEGER DEFAULT 1\n        )"
  },
  {
    "name": "search_history",
    "sql": "CREATE TABLE search_history (\n            history_id      TEXT PRIMARY KEY,\n            user_id         TEXT,\n            query           TEXT NOT NULL,\n            results_count   INTEGER,\n            clicked_doc_id  TEXT,\n            locale          TEXT,\n            created_at      TEXT NOT NULL\n        )"
  }
]
